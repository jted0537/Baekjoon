#include<iostream>
#include<map>
#include<set>
#include<queue>
#include<vector>
#include<algorithm>
#include<string>
#include<stack>
using namespace std;
int main() {
    vector<int> v1;
    v1.push_back(5);
    v1.push_back(4);
    v1.push_back(3);
    v1.push_back(2);
    v1.push_back(1);
    string s1;
    stack<char> s2;
    s2.
    do{
        for(vector<int>::iterator it = v1.begin(); it != v1.end(); it++) {
            cout << *it << " ";
        }
        cout << endl;
    } while(next_permutation(v1.begin(), v1.end()));
}